import Head from 'next/head';
import Layout from '../layout/layout';

const Referentiels = () => {
    return (
        <Layout>
            <Head>
                <title>Green Data For Health | Référentiels</title>
            </Head>
        </Layout>
    );
};

export default Referentiels;